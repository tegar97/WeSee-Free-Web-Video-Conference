import React from 'react';
import { ChatBarContainer } from './ChatBar.styles';
const ChatBar = () => {
    return (
        <ChatBarContainer>
            <h1>ads</h1>
        </ChatBarContainer>
    );
};

export default ChatBar;
