import React from 'react';
import { ChatBarContainer } from './ChatBar.styles';
const ChatBar = () => {
    return (
        <ChatBarContainer>
            <div>text message</div>
        </ChatBarContainer>
    );
};

export default ChatBar;
